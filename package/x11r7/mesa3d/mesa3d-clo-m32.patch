[PATCH] Fix unrecognized command line option -m32
Signed-off-by: Anatol Derksen <derksen@fs-net.de>
---

--- a/bin/mklib
+++ b/bin/mklib
@@ -335,7 +335,7 @@
 	    set ${OBJECTS}
 	    ABI32=`file $1 | grep 32-bit`
 	    if [ "${ABI32}" -a `uname -m` = "x86_64" ] ; then
-		OPTS="-m32 ${OPTS}"
+		OPTS=" ${OPTS}"
 	    fi
 
             if [ "${ALTOPTS}" ] ; then
